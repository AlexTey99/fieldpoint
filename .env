# MOCK APP — these values are intentionally committed so the app boots anywhere
# (local, Docker, Atlantic Software Factory previews) with zero setup.
# Never reuse this secret in a real deployment.
PORT=4100
HOST=0.0.0.0
DB_PATH=./data/fieldpoint.db
SESSION_SECRET=fieldpoint-demo-secret-0f3c9a1e7b2d4c6f8a0b1c2d3e4f5a6b7c8d9e0f
SESSION_TTL_HOURS=72
NODE_ENV=development
# Create demo accounts + sample sites on first boot (idempotent).
SEED_DEMO=true
